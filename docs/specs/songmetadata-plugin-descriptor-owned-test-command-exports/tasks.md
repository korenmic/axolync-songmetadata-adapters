# SongMetadata Plugin Descriptor-Owned Test Command Exports Tasks

- [ ] 1. Publish SongMetadata plugin descriptor command or zero-test metadata.
  - Add build, sanity, and full-test command exports if applicable.
  - Otherwise encode descriptor-owned zero-test status.

- [ ] 2. Preserve SongMetadata plugin catalog/source role.
  - Confirm no runtime authority is introduced.
  - Keep catalog metadata discoverable.

- [ ] 3. Validate SongMetadata plugin descriptor warning cleanup.
  - Run focused command discovery validation.
  - Confirm SongMetadata plugin fallback warnings disappear.
