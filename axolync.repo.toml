schema = "axolync.repo/v1"

[repo]
id = "axolync-songmetadata-plugin"
roles = ["consumable"]
display_name = "Axolync SongMetadata Plugin"
description = "Shared SongMetadata adapter catalog and provider migration support."

[source]
git_url = "https://github.com/korenmic/axolync-songmetadata-plugin.git"
default_ref = "master"
local_dev_path_hint = "../axolync-songmetadata-plugin"

[bootstrap]
install_mode = "managed-clone"
install_root = ".axolync/repos"
readme = "README.md"
agent_docs = ["README.md"]

[exports.docs]
bootstrap = ["README.md"]

[[consumes.repos]]
id = "axolync-contract"
use = "schema-and-runtime-contracts"
required = false
source = "managed"
